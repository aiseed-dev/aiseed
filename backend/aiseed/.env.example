# Claude Agent SDK Configuration
# Claude Code を使用するため、API キーは不要です
# 初回起動時に Claude Console または Claude App でログインしてください
# 詳細: https://code.claude.com/docs/ja/setup

# Database
DATABASE_URL=postgresql+asyncpg://aiseed:password@localhost/aiseed

# Server
HOST=0.0.0.0
PORT=8000
WORKERS=4

# Rate Limiting (default values, overridden by database)
DEFAULT_RATE_LIMIT_FREE=10
DEFAULT_RATE_LIMIT_PREMIUM=100
